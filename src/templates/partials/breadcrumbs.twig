<li class="breadcrumbs__item">
	{% if loop.last %}
		<span class="breadcrumbs__item-link breadcrumbs__item-link--active">{{ item.text }}</span>
	{% else %}
		<a href="{{ item.link }}" class="breadcrumbs__item-link">{{ item.text }}</a>
	{% endif %}
</li>
