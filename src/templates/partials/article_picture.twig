{% import "ui/picture.twig" as ui %}

<figure class="picture">

	{{ ui.picture(
			"picture__image",
			item.src,
			item.sources,
			item.alt,
			item.width,
			item.height,
			"center",
			"cover"
		) }}

	<figcaption class="picture__title">{{ item.title }}</figcaption>
</figure>