<div class="field-checkbox">
	<input 
        type="checkbox" 
        id="{{ item.id }}" 
        name="{{ item.name }}" 
        value="{{ item.value }}" 
        class="field-checkbox__input" 
        {% if item.required == "yes" %} required {% endif %}
    />
	<label 
        for="{{ item.id }}" 
        class="label-default"
    >
		{{ item.label }}
	</label>
</div>
